services:
  - type: web
    name: clipboard-sync-relay
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.14.0
    healthCheckPath: /
